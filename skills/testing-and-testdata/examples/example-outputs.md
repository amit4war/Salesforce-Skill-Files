# Example Outputs

- Added `AccountServiceTest` with 7 scenario-based test methods.
- Added `TestDataFactory.createAccounts(Integer count, Boolean doInsert)`.
- Covered positive, negative, single, bulk, and runAs security paths.
- Verified persisted outcomes with post-DML queries.
