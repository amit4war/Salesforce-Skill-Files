# Authoring Rules

- State the storage decision explicitly.
- Keep one provider path per configuration area.
- Use typed getters with safe defaults or documented exceptions.
- Prefer reusable config models over scattered direct queries.
