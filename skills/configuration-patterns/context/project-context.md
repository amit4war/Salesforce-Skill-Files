# Project Context

This skill governs shared configuration across Salesforce metadata and code.

## In Scope

- Business config in Custom Metadata
- Hierarchy overrides and bypass settings
- Integration endpoints and credentials
- UI labels and reusable messages

## Core Principle

Configuration must be secure, centralized, and deployable where appropriate.
