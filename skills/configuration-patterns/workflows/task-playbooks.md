# Task Playbooks

## Feature Toggle

- Use Custom Metadata for deployable toggles.
- Add typed provider getter.
- Update callers and tests.

## Bypass Control

- Use Hierarchy Custom Setting.
- Check bypass early in trigger/flow paths.
- Test both on and off behavior.

## Integration Endpoint

- Use Named Credential.
- Keep secrets out of source control.
- Document per-environment setup.
